class Tile {
    constructor(pos, size, name, texturePos, textureSpeed) {
        this.pos = pos;
        this.size = size;
        this.name = name;
        this.texturePos = texturePos;
        this.textureSpeed = textureSpeed;
    }
}
